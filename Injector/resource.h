#pragma once

// Resource IDs shared between Injector.cpp and the generated resource script
// (EmbeddedDll.rc, produced by Injector.vcxproj or by make_embedded_rc.cmake).
// The generated script inlines the same value; keep the two in sync.
#define IDR_EMBEDDED_DLL 101
